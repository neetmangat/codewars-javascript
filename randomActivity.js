const activities = ['playing tennis', 'driving', 'smoking', 'playing basketball', 'playing baseball', 'getting food', 'shopping', 'taking a shit'];

console.log(activities[Math.floor(Math.random() * activities.length)])